import Title from "@/app/components/title";

export const dynamic = "force-static";

const PageReminder = () => {
  return (
    <div>
      <Title title="What can I remider you of ?"></Title>
    </div>
  );
};

export default PageReminder;
