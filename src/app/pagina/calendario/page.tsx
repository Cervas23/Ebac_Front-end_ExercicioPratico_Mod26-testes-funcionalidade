import Title from "@/app/components/title";

export const dynamic = "force-static";

const PageCalendar = () => {
  return (
    <div>
      <Title title="Write down anything you need to do..."></Title>
    </div>
  );
};

export default PageCalendar;
